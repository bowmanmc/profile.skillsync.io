<!DOCTYPE html>
<html lang="en">

<% include partials/head %>

<body>

    <% include partials/svg %>

    <!-- Header Box -->
    <header class="header">

        <div class="headshot"></div>

        <h1><%= account.name %></h1>
        <span class="header__title"><%= resume.category %></span>

        <% include partials/social %>
    </header>
    <!-- /Header Box -->

    <div class="resume">

        <section class="skills">
            <h2>
                <span class="section__icon">
                    <svg>
                        <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#icon-list2"></use>
                    </svg>
                </span>
                Skills
            </h2>
            <ul>
                <% resume.skills.forEach(function(skill) { %>
                    <li><%= skill %></li>
                <% }) %>
            </ul>
        </section>

        <section class="summary">
            <h2>
                <span class="section__icon">
                    <svg>
                        <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#icon-address-book"></use>
                    </svg>
                </span>
                Profile
            </h2>
            <p>
                <%= resume.summary %>
            </p>
        </section>

        <section class="projects">
            <h2>
                <span class="section__icon">
                    <svg>
                        <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#icon-embed2"></use>
                    </svg>
                </span>
                Projects
            </h2>

            <% resume.projects.forEach(function(project) { %>
                <div class="item">
                    <h3><%= project.name %></h3>
                    <p><%= project.description %></p>
                    <ul class="skilllist">
                        <% project.skills.forEach(function(skill) { %>
                            <li><%= skill %></li>
                        <% }) %>
                    </ul>
                    <ul class="links">
                        <li>
                            <a href="<%= project.repo %>">
                                <svg class="link__icon">
                                    <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#icon-github"></use>
                                </svg>
                                <span class="link-txt"><%= project.repo %></span>
                            </a>
                        </li>
                        <li>
                            <a href="<%= project.url %>">
                                <svg class="link__icon">
                                    <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#icon-sphere"></use>
                                </svg>
                                <span class="link-txt"><%= project.url %></span>
                            </a>
                        </li>
                    </ul>
                </div>
            <% }) %> <!-- resume.projects.forEach -->
        </section>

        <section class="work">
            <h2>
                <span class="section__icon">
                    <svg>
                        <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#icon-office"></use>
                    </svg>
                </span>
                Employment History
            </h2>
            <% resume.work.forEach(function(job) { %>
                <div class="item">
                    <h3><%= job.name %></h3>
                    <p><%= job.duration %></p>
                    <p><%= job.summary %></p>
                    <ul class="links">
                        <li>
                            <a href="<%= job.url %>">
                                <svg class="link__icon">
                                    <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#icon-sphere"></use>
                                </svg>
                                <span class="link-txt"><%= job.url %></span>
                            </a>
                        </li>
                    </ul>
                </div>
            <% }) %>
        </section>

        <section class="education">
            <h2>
                <span class="section__icon">
                    <svg>
                        <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#icon-books"></use>
                    </svg>
                </span>
                Education and Certifications
            </h2>
            <% resume.education.forEach(function(school) { %>
                <div class="item">
                    <h3><%= school.name %></h3>
                    <p><%= school.duration %></p>
                    <p><%= school.credential %></p>
                </div>
            <% }) %>
        </section>
    </div><!-- resume -->

</body>

</html>
